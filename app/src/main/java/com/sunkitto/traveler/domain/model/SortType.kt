package com.sunkitto.traveler.domain.model

/**
 * Enumeration of possible Sorting options for equipments.
 */
enum class SortType {
    NONE,
    HIGHEST_PRICE,
    LOWEST_PRICE,
    HIGHEST_NAME,
    LOWEST_NAME,
}